<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dataserver.mapper.SpotMapper">
    <resultMap type="com.example.dataserver.entity.Spot" id="SpotResult">
        <id     property="id"         column="id"        />
        <result property="spotName"       column="spot_name"      />
    </resultMap>
    <resultMap type="com.example.dataserver.entity.Notice" id="NoticeResult">
        <id     property="id"         column="id"        />
        <result property="noticeTitle"       column="notice_title"      />
        <result property="noticeContent"       column="notice_content"      />
        <result property="imageUrl"       column="image_url"      />
    </resultMap>
    <select id="getSpot" resultMap="SpotResult">
        select * from scenic_spot
    </select>
    <select id="getNotice" resultMap="NoticeResult">
        select * from scenic_notice
    </select>
</mapper>
